<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.example.horoscope" 
	android:versionCode="1"
	android:versionName="1.0">
	<uses-sdk android:minSdkVersion="8" />

	<application android:icon="@drawable/icon" android:label="@string/app_name">

		<activity android:name=".TabViewActivity" android:theme="@android:style/Theme.NoTitleBar"
			android:label="@string/app_name">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<activity android:name=".ListViewActivity"
			android:label="List" />

		<activity android:name=".TableLayoutActivity" 
			android:label="Table" />

		<activity android:name=".GridViewActivity"
			android:label="Grid" />
			
		<activity android:name=".RelativeLayoutActivity"
			android:label="Relative" />

		<activity android:name=".LinearLayoutActivity" 
			android:label="Horoscope Details"></activity>
	</application>
</manifest>